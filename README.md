# Project StockNet

Stock Backend App.


## MakeFile

Run the cli
```bash
make run
```

Clean up binary from the last build:
```bash
make clean
```

```
sudo su - postgres
export PATH=$PATH:/data/pg18/bin
pg_ctl -D /data/pg18/data start 
```